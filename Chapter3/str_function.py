name = "Prabhav"

print (len(name)) # gives the length of the string
print (name.endswith("av")) # checks if the string ends with "v"
print (name.startswith("Pr")) # checks if the string starts with "Pr"
print (name.startswith("pr")) #(case-sensitive, so it returns False)
